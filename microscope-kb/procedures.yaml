# Standard Operating Procedures
#
# Common workflows and acquisition sequences.
# Agents should understand these before modifying workflow code.

procedures:
  startup:
    description: "Standard microscope startup sequence"
    steps:
      - "Initialize DAQ device and verify connection"
      - "Home all stages to known positions"
      - "Verify safety interlocks are engaged"
      - "Initialize detectors"
      - "Configure default scan parameters"
      # TODO: Add your specific startup sequence

  shutdown:
    description: "Standard microscope shutdown sequence"
    steps:
      - "Stop any running acquisitions"
      - "Disable all laser outputs"
      - "Return stages to home/park position"
      - "Close DAQ tasks"
      - "Disconnect all hardware"
      # TODO: Add your specific shutdown sequence

  acquisition: {}
    # confocal_scan:
    #   description: "Standard confocal point scanning"
    #   steps:
    #     - "Set scan parameters (area, resolution, speed)"
    #     - "Configure galvo waveforms"
    #     - "Set laser power and detector gain"
    #     - "Start synchronized DAQ output and input"
    #     - "Collect and reconstruct image"

  calibration: {}
    # stage_calibration:
    #   description: "Verify stage positioning accuracy"
    #   frequency: "monthly"
    #   steps: []
