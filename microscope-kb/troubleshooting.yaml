# Troubleshooting Guide
#
# Known issues, workarounds, and diagnostic procedures.
# Agents should check here before proposing fixes for known problems.

troubleshooting:
  known_issues: []
    # - issue: "DAQ task fails to start after previous crash"
    #   symptoms: ["nidaq error -200288", "resource already reserved"]
    #   cause: "Previous DAQ task was not properly closed"
    #   workaround: "Reset the DAQ device in NI MAX, or restart the application"
    #   permanent_fix: "Ensure all DAQ tasks are closed in finally blocks"

  common_errors: {}
    # serial_timeout:
    #   error: "Serial communication timeout"
    #   possible_causes:
    #     - "Wrong COM port"
    #     - "Device not powered on"
    #     - "Baud rate mismatch"
    #   diagnostic_steps:
    #     - "Verify device is powered and connected"
    #     - "Check Device Manager for correct COM port"
    #     - "Try communication with manufacturer's software"

  diagnostic_procedures: {}
    # hardware_connectivity:
    #   description: "Verify all hardware devices are reachable"
    #   steps:
    #     - "Run connection test for each configured device"
    #     - "Log response times and firmware versions"
    #     - "Report any devices that fail to respond"
