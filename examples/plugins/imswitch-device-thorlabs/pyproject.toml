[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "imswitch-device-thorlabs"
version = "0.2.0"
description = "Thorlabs device support (TSI cameras, Kinesis stages) for ImSwitch2, extracted from the ImSwitch core"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "GPL-3.0-or-later" }
keywords = ["imswitch", "imswitch-plugin", "thorlabs", "camera", "stage", "positioner"]
classifiers = [
    "License :: OSI Approved :: GNU General Public License v3 or later (GPLv3+)",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering",
]
dependencies = [
    "imswitch2>=0.1",
    "numpy",
]

[project.optional-dependencies]
# Vendor SDKs — only needed to talk to real hardware. The mock paths work
# without them: TSI camera via cameraSerial "MOCK_...", Kinesis stage via
# managerProperties useMock=true.
hardware = ["thorlabs-tsi-sdk", "pylablib"]
test = ["pytest", "jsonschema"]

[project.entry-points."imswitch.manifest"]
imswitch-device-thorlabs = "imswitch_device_thorlabs:imswitch.json"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
imswitch_device_thorlabs = [
    "imswitch.json",
    "schemas/*.schema.json",
    "setup_templates/*.json",
]
