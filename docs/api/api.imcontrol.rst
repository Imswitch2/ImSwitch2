*************
api.imcontrol
*************

.. This page is generated by tools/generateapidocs.py; edit the
   docstrings in the controllers, not this file.

.. class:: api.imcontrol

   These functions are available in the ``api.imcontrol`` object of a
   script.  Which ones a session has depends on the widgets its setup
   file loads; each entry says what it needs.  Calling one the setup
   does not provide raises ``AttributeError`` with that explanation.

   .. method:: acquireImage() -> None

      Always available.

   .. method:: activateRotScan(activate: bool)

      API callable method to activate or deactivate the scan.

      :param activate: Boolean value indicating whether to activate (True) or deactivate (False) the scan.

      Needs *RotationScan* in ``availableWidgets``.

   .. method:: autoFocus(rangez: float = 100.0, resolutionz: float = 10.0) -> None

      Start an autofocus scan asynchronously.

      Scans +-rangez/2 around the current Z in steps of resolutionz,
      fits a parabola to the gradient-variance focus metric, and moves
      to the estimated peak.

      Needs *Autofocus* in ``availableWidgets``.

   .. method:: buildWorkflowFacade(**kwargs: Any) -> Any

      Build a MicroscopeFacade from the current master controller.

      ImSwitch's ``generateAPI`` flattens every ``@APIExport``'d method to
      the top level of ``api.imcontrol`` — so this method is reachable as
      ``api.imcontrol.buildWorkflowFacade(...)`` (not via a sub-namespace).

      :param \*\*kwargs: Forwarded to ``build_facade_from_master``. Common arguments:

                         - ``laser_aliases`` (dict[str, str]): logical → setup laser names
                         - ``detector_name`` (str): name of detector/camera to expose
                         - ``xy_positioner_name`` (str): name of XY positioner
                         - ``z_positioner_name`` (str): name of Z positioner
                         - ``time_resolved_detector_name`` (str): detector implementing
                           the time-resolved contract
                         - ``hwp_name`` / ``qwp_name`` (str): rotator names
                         - ``hwp_presets`` / ``qwp_presets`` (RotatorPresets)
                         - ``scan_workflow`` / ``scan_done_signal``: optional overrides;
                           by default these are taken from the communication channel

      :returns: facade object with WFS-shaped sub-facades.
      :rtype: MicroscopeFacade

      .. rubric:: Example

      >>> facade = api.imcontrol.buildWorkflowFacade(
      ...     laser_aliases={'488': '488 (EXC) sn27311',
      ...                    '405': '405 (ACT) sn26647'},
      ...     detector_name='Kiralux',
      ...     xy_positioner_name='XY',
      ...     z_positioner_name='Z',
      ...     hwp_name='HWP', qwp_name='QWP',
      ... )
      >>> facade.laser_con.set_constant_power(['488'], [50.0])

      Always available.

   .. method:: changed3StepDelayPar(d3StepDelay)

      Needs the *Scan* widget (PointScan) in ``availableWidgets``.

   .. method:: changeRotationParameters(rotationPars)

      Needs *RotationScan* in ``availableWidgets``.

   .. method:: changeScanCenterPos(positionerName, positionerScanCenterPos)

      Needs the *Scan* widget (Advanced, PointScan) in ``availableWidgets``.

   .. method:: changeScanPower(laserName: str, laserValue: Union[int, float]) -> None

      Sets the value of the specified laser, in the units that the laser
      uses (alias of setLaserValue kept for existing scripts). Runs on the
      UI thread because it updates the laser widget.

      Needs *Laser* in ``availableWidgets``.

   .. method:: changeScanSize(positioner: str, size: float)

      Needs the *Scan* widget (Advanced, PointScan) in ``availableWidgets``.

   .. method:: deleteSetupMode(name)

      Delete a saved setup mode. Returns True if a file was removed.

      Always available.

   .. method:: detectCellTargets() -> numpy.ndarray

      Segment cells in the current overview and show target markers.

      This GUI-safe path does not move the stage. It only updates the cached
      target list and the overview marker overlay.

      Needs *Tiling* in ``availableWidgets``.

   .. method:: duplicateSetupMode(name, newName, description=None, shortcut='')

      Duplicate a saved setup mode and return the new mode.

      Always available.

   .. method:: get_flip_state(name)

      Return current state of one flip mirror.

      Needs *FlipMirror* in ``availableWidgets``.

   .. method:: getDetectorNames() -> List[str]

      Returns the device names of all detectors. These device names can
      be passed to other detector-related functions.

      Needs *Settings* in ``availableWidgets``.

   .. method:: getDetectorParameter(detectorName: str, parameterName: str) -> Any

      Returns the value of the specified detector-specific parameter, in
      the parameter's own units -- the value the Settings widget shows.
      Parameter names and units differ from detector to detector;
      getDetectorParameters lists them. Raises AttributeError for a name
      the detector does not have.

      Needs *Settings* in ``availableWidgets``.

   .. method:: getDetectorParameters(detectorName: str) -> Dict[str, Dict[str, Any]]

      Returns all detector-specific parameters of the specified detector
      as {parameter name: {'value', 'units', 'editable', 'options'}}. 'units'
      is None for a parameter that picks from a list of 'options', and
      'options' is None for a numerical one. Only an editable parameter can
      be changed with setDetectorParameter.

      Needs *Settings* in ``availableWidgets``.

   .. method:: getFileName()

      Gets the filename of the data to save.

      Needs *Recording* in ``availableWidgets``.

   .. method:: getLaserNames() -> List[str]

      Returns the device names of all lasers. These device names can be
      passed to other laser-related functions.

      Needs *Laser* in ``availableWidgets``.

   .. method:: getPositionerNames() -> List[str]

      Returns the device names of all positioners. These device names can
      be passed to other positioner-related functions.

      Needs *Positioner* or *WellPlate* in ``availableWidgets``.

   .. method:: getPositionerPositions() -> Dict[str, Dict[str, float]]

      Returns the positions of all positioners.

      Needs *Positioner* or *WellPlate* in ``availableWidgets``.

   .. method:: getRecFileFormat() -> str

      Returns the file format recordings are saved in: 'HDF5', 'TIFF'
      or 'ZARR'.

      Needs *Recording* in ``availableWidgets``.

   .. method:: getRecFolder() -> str

      Needs *Recording* in ``availableWidgets``.

   .. method:: getRegistrationSummary() -> str

      Diagnostics from the last run with tile alignment enabled.

      Needs *Tiling* in ``availableWidgets``.

   .. method:: getScanRequestStatus(requestId: str) -> dict

      Status of a scan started with runScan, as ``{requestId, source,
      state, message, exact}`` with state ``pending``, ``succeeded`` or
      ``failed``. Raises KeyError for an unknown or evicted request id.

      Always available.

   .. method:: getScanSourceNames() -> List[str]

      Widget keys of every scan controller that runScan(source=...) can
      target on this setup.

      Always available.

   .. method:: getSelectedDetectors() -> list

      Names of the detectors currently selected for acquisition.

      Needs *View* in ``availableWidgets``.

   .. method:: getSessionNote() -> str

      Return the free-text note attached to new recordings ('' if none).

      Always available.

   .. method:: getSetupMode(name)

      Return a saved setup mode by name.

      Always available.

   .. method:: getSetupModeComponentLabel(componentName)

      Return a human-readable label declared by a setup-mode component.

      Always available.

   .. method:: getSetupModeComponents()

      Return component names that currently support setup modes.

      Always available.

   .. method:: getSetupModeStorageDir()

      Return the directory where setup mode JSON files are stored.

      Always available.

   .. method:: getStitchedImage() -> Optional[numpy.ndarray]

      Needs *Tiling* in ``availableWidgets``.

   .. method:: isRecording() -> bool

      Whether a recording is currently active.

      Needs *Recording* in ``availableWidgets``.

   .. method:: isSetupModeHardwareCritical(componentName)

      Return whether a setup-mode component changes safety-critical hardware.

      Always available.

   .. method:: listSetupModes()

      Return names of all saved setup modes.

      Always available.

   .. method:: loadCalibration(calibname=None)

      Load calibration data, to be used to interpolate the same spline interpolation in a new instance.

      Needs *RotationScan* in ``availableWidgets``.

   .. method:: loadParams()

      Needs *SLM* in ``availableWidgets``.

   .. method:: loadScanParamsFromFile(filePath: str) -> None

      Loads scanning parameters from the specified file.

      Needs *TriggerScopeRaster* or the *Scan* widget (Advanced, Base, MoNaLISA, PointScan) in ``availableWidgets``.

   .. method:: loadSetupMode(name, componentNames=None)

      Apply a saved setup mode.

      Thin wrapper over :meth:`applySetupMode` preserving the legacy return
      value so the interactive ``SetupModesController`` and existing tests are
      unchanged.

      :param name: Saved mode name.
      :param componentNames: Optional iterable of widget/component keys. If
                             omitted, the mode's saved component list is applied.

      :returns: list of warning strings.

      Always available.

   .. method:: move_flip(name, state)

      Move one flip mirror and all linked followers to state 0 or 1.

      Needs *FlipMirror* in ``availableWidgets``.

   .. method:: moveAbs(name, pos)

      Needs *Rotator* in ``availableWidgets``.

   .. method:: movePositioner(positionerName: str, axis: str, dist: float) -> None

      Moves the specified positioner axis by the specified number of
      micrometers.

      Needs *Positioner* or *WellPlate* in ``availableWidgets``.

   .. method:: post_json(path: str, payload: dict) -> str

      Sends the specified command to the RS232 device and returns a
      string encoded from the received bytes.

      Needs *Laser* in ``availableWidgets``.

   .. method:: renameSetupMode(name, newName)

      Rename a saved setup mode and return the updated mode.

      Always available.

   .. method:: reset_connections()

      Close/reopen all flip mirror connections and re-sync followers.

      Needs *FlipMirror* in ``availableWidgets``.

   .. method:: runCellTargeting(feature_callback: Optional[Callable[[int, dict, Tuple[float, float]], NoneType]] = None, move_only: bool = False) -> None

      Move through detected cells and optionally run a per-cell workflow.

      :param feature_callback: Optional ``f(idx, props_for_cell, stage_xy)``
                               invoked after moving to each accepted cell.
      :param move_only: If True, move through targets even without a callback.

      If neither ``feature_callback`` nor ``move_only`` is supplied this
      method behaves like ``detectCellTargets()`` and does not move hardware.

      Needs *Tiling* in ``availableWidgets``.

   .. method:: runScan(source: Optional[str] = None) -> imswitch.imcontrol.model.scan_request.ScanRunHandle

      Starts one scan with the parameters set in the scan widget and
      returns a handle for its completion.

      The request is pre-flighted before any lifecycle signal is published;
      a refused start (a scan is already running, the previous one is still
      finishing, ...) raises ``ScanRequestRejectedError`` (a RuntimeError)
      whose message is the reason, and nothing else happens. A scan design
      the scan manager refuses (longer than ``scan.maxScanTimeMin``, a
      scanner driven outside its voltage range) raises the same way; it is
      found only after ``scanStarting`` went out, which is then paired with
      a ``scanEnded``. On rigs with several scanners ``source``
      selects one by its widget key (see getScanSourceNames); without it the
      canonical Scan controller or a lone capable controller is used, and
      ambiguity raises. Repeat is switched off for the scan.

      The returned handle resolves for exactly this scan whatever the order
      of any waiter: ``handle.wait(timeout)`` (from a script), ``handle.done``
      / ``handle.successful`` / ``handle.message``, or
      ``getScanRequestStatus(handle.requestId)`` (remote clients receive the
      handle as ``{requestId, source, state, message}``).

      Always available.

   .. method:: saveScanParamsToFile(filePath: str) -> None

      Saves the set scanning parameters to the specified file.

      Needs *TriggerScopeRaster* or the *Scan* widget (Advanced, Base, MoNaLISA, PointScan) in ``availableWidgets``.

   .. method:: saveSetupMode(name, componentNames=None, description=None, shortcut=None)

      Snapshot selected setup-mode components and save them as name.

      :param name: Mode name.
      :param componentNames: Optional iterable of widget/component keys. If
                             omitted, all currently supported setup-mode components are
                             saved.
      :param description: Optional user description. If omitted while
                          overwriting an existing mode, the previous description is
                          preserved.
      :param shortcut: Optional shortcut text. If omitted while overwriting an
                       existing mode, the previous shortcut is preserved. Pass an
                       empty string to clear the shortcut.

      :returns: dict with keys "mode" and "warnings".

      Always available.

   .. method:: send_serial(payload: str) -> str

      Sends the specified command to the RS232 device and returns a
      string encoded from the received bytes.

      Needs *Laser* in ``availableWidgets``.

   .. method:: sendTrigger(triggerId: int)

      Sends a trigger puls through external device

      Needs *Laser* in ``availableWidgets``.

   .. method:: set_link(follower_name, master_name)

      Set or remove follower -> master link.

      Needs *FlipMirror* in ``availableWidgets``.

   .. method:: setDetectorBinning(detectorName: str, binning: int) -> None

      Sets binning value for the specified detector.

      Needs *Settings* in ``availableWidgets``.

   .. method:: setDetectorParameter(detectorName: str, parameterName: str, value: Any) -> None

      Sets the specified detector-specific parameter to the specified
      value.

      Needs *Settings* in ``availableWidgets``.

   .. method:: setDetectorROI(detectorName: str, frameStart: Tuple[int, int], shape: Tuple[int, int]) -> None

      Sets the ROI for the specified detector. frameStart is a tuple
      (x0, y0) and shape is a tuple (width, height).

      Needs *Settings* in ``availableWidgets``.

   .. method:: setDetectorSelected(detectorName: str, selected: bool) -> bool

      Select or deselect a detector for ImSwitch-managed acquisition.

      Deselected detectors are left out of live view and of scans. A
      detector an explicit consumer is holding — a recording, a workflow, an
      event modality — still runs regardless, so this cannot silently break
      an acquisition already under way.

      Returns False when the change is queued because a scan currently owns
      the detector; it is applied at the next scan iteration.

      Needs *View* in ``availableWidgets``.

   .. method:: setDetectorToRecord(detectorName: Union[List[str], str, int], multiDetectorSingleFile: bool = False) -> None

      Sets which detectors to record. One can also pass -1 as the
      argument to record the current detector, or -2 to record all detectors.

      Needs *Recording* in ``availableWidgets``.

   .. method:: setLaserActive(laserName: str, active: bool) -> None

      Sets whether the specified laser is powered on.

      Needs *Laser* in ``availableWidgets``.

   .. method:: setLaserValue(laserName: str, value: Union[int, float]) -> None

      Sets the value of the specified laser, in the units that the laser
      uses.

      Needs *Laser* in ``availableWidgets``.

   .. method:: setLiveViewActive(active: bool) -> None

      Sets whether the LiveView is active and updating.

      Needs *View* in ``availableWidgets``.

   .. method:: setMask(maskMode)

      Needs *SLM* in ``availableWidgets``.

   .. method:: setMotorsEnabled(positionerName: str, is_enabled: int) -> None

      Moves the specified positioner axis to the specified position.

      Needs *Positioner* in ``availableWidgets``.

   .. method:: setPositioner(positionerName: str, axis: str, position: float) -> None

      Moves the specified positioner axis to the specified position.

      Needs *Positioner* or *WellPlate* in ``availableWidgets``.

   .. method:: setPositionerSpeed(positionerName: str, speed: float) -> None

      Moves the specified positioner axis to the specified position.

      Needs *Positioner* in ``availableWidgets``.

   .. method:: setPositionerStepSize(positionerName: str, stepSize: float) -> None

      Sets the step size of the specified positioner to the specified
      number of micrometers.

      Needs *Positioner* or *WellPlate* in ``availableWidgets``.

   .. method:: setRecFileFormat(fileFormat: str) -> None

      Sets the file format recordings are saved in: 'HDF5', 'TIFF' or
      'ZARR' (any case) -- the Recording widget's "File format". Raises
      ValueError for any other name, and RuntimeError while snaps are set
      to go to the image display, which fixes the format to TIFF.

      Needs *Recording* in ``availableWidgets``.

   .. method:: setRecFilename(filename: Optional[str]) -> None

      Sets the name of the file to record to. This only sets the name of
      the file, not the full path. One can also pass None as the argument to
      use a default time-based filename.

      Needs *Recording* in ``availableWidgets``.

   .. method:: setRecFolder(folderPath: str) -> None

      Sets the folder to save recordings into.

      Needs *Recording* in ``availableWidgets``.

   .. method:: setRecModeCameraTimelapse(framesToRec: int, intervalSeconds: Union[int, float], timelapseSingleFile: bool = False) -> None

      Configure a camera-only timelapse of discrete one-frame captures.

      Needs *Recording* in ``availableWidgets``.

   .. method:: setRecModeScanOnce() -> None

      Sets the recording mode to record a single scan.

      Needs *Recording* in ``availableWidgets``.

   .. method:: setRecModeScanTimelapse(lapsesToRec: int, freqSeconds: float, timelapseSingleFile: bool = False) -> None

      Sets the recording mode to record a timelapse of scans.

      Needs *Recording* in ``availableWidgets``.

   .. method:: setRecModeSpecFrames(numFrames: int) -> None

      Sets the recording mode to record a specific number of frames.

      Needs *Recording* in ``availableWidgets``.

   .. method:: setRecModeSpecTime(secondsToRec: Union[int, float]) -> None

      Sets the recording mode to record for a specific amount of time.

      Needs *Recording* in ``availableWidgets``.

   .. method:: setRecModeUntilStop() -> None

      Sets the recording mode to record until recording is manually
      stopped.

      Needs *Recording* in ``availableWidgets``.

   .. method:: setSessionNote(note: str) -> None

      Set the free-text note attached to recordings made from now on.

      The same text the Tools -> Session notes... dialog edits. Recordings
      snapshot the shared attributes when they start, so this reaches every
      file saved after the call and none saved before it. Pass '' to clear.

      Always available.

   .. method:: setSnapModeSave(mode='tiff') -> None

      Sets the file format snaps are saved in: 'HDF5', 'TIFF' or 'ZARR'
      (any case). Raises ValueError for any other name.

      Needs *Recording* in ``availableWidgets``.

   .. method:: setSpecifyFileName(enable=True) -> None

      Needs *Recording* in ``availableWidgets``.

   .. method:: setTileLabel(label: str) -> None

      Needs *Tiling* in ``availableWidgets``.

   .. method:: signals() -> Mapping[str, imswitch.imcommon.framework.qt.Signal]

      Returns signals that can be used with e.g. the getWaitForSignal
      action. Currently available signals are:

       - acquisitionStarted
       - acquisitionStopped
       - recordingStarted
       - recordingEnded
       - recordingFailed
       - scanStarting (the run-level start, before hardware arms)
       - scanStarted (the execution backend started the iteration)
       - scanDone (an iteration finished)
       - scanEnded (the run is over, on every terminal path)
       - scanRejected(reason) (a start request was refused and its scan
         never runs; reason says why)

      They can be accessed like this: api.imcontrol.signals().scanEnded

      Always available.

   .. method:: snapImage(output: bool = False) -> Optional[numpy.ndarray]

      Take a snap and save it to a .tiff file at the set file path.

      Needs *Recording* in ``availableWidgets``.

   .. method:: snapshotSetupModeState(componentNames=None)

      Snapshot selected setup-mode components without saving them.

      Always available.

   .. method:: startRecording() -> None

      Starts recording with the set settings to the set file path.

      Needs *Recording* in ``availableWidgets``.

   .. method:: startTiling() -> None

      Needs *Tiling* in ``availableWidgets``.

   .. method:: stepPositionerDown(positionerName: str, axis: str) -> None

      Moves the specified positioner axis in negative direction by its
      set step size.

      Needs *Positioner* or *WellPlate* in ``availableWidgets``.

   .. method:: stepPositionerUp(positionerName: str, axis: str) -> None

      Moves the specified positioner axis in positive direction by its
      set step size.

      Needs *Positioner* or *WellPlate* in ``availableWidgets``.

   .. method:: stopRecording() -> bool

      Stops recording. Idempotent: returns True if a recording was
      active and its stop was requested (``recordingEnded`` or
      ``recordingFailed`` will follow), False if nothing was recording (no
      signal will follow, so do not wait for one).

      Needs *Recording* in ``availableWidgets``.

   .. method:: stopTiling() -> None

      Needs *Tiling* in ``availableWidgets``.

   .. method:: toggleSLMDisplay(enabled)

      Needs *SLM* in ``availableWidgets``.

   .. method:: updateSetupModeMetadata(name, description=None, shortcut=None)

      Update user-editable metadata for a saved setup mode.

      Always available.
