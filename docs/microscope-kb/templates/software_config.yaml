# Template — DO NOT fill in here.
# Copy this file to your LOCAL microscope-kb directory (e.g.
# ~/microscope-kb/my-system/) and fill it in there.  This
# template is checked in for reference only; no KB content
# ships with ImSwitch.
#
# Schema reference: ../schemas.md
# Generation workflow: ../README.md

# TODO: This file can be auto-seeded from ImSwitch setup JSON using
#       utility_scripts/scopeaid_seed_from_config.py (planned)

version: null
last_updated: null  # YYYY-MM-DD

software_config:
  source_file: null  # Original config file name
  software: null  # ImSwitch / MicroManager / ZEN / LAS X / etc.

  how_to_interpret: []

  guidance:
    general: []  # What's safe to change
    safety_critical: []  # What must NOT be changed without expert knowledge

  # Include only sections relevant to your control software
  # The structure should mirror the software's own config hierarchy
  detectors: {}
  lasers: {}
  positioners: {}
  scan_defaults: {}
  roi_presets: {}
  acquisition_presets: {}
