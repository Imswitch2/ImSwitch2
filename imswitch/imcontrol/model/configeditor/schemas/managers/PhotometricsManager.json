{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "properties": {
    "Photometrics": {
      "description": "Dictionary of detector-parameter names (e.g. ``\"Set exposure time\"``) → values applied via ``setParameter`` after the camera is opened.  If the key is absent no defaults are pushed.",
      "type": "object",
      "x-imswitch-kind": "object",
      "x-imswitch-source": [
        "code:mapping-use",
        "code:optional(in)",
        "docs:object"
      ]
    },
    "cameraListIndex": {
      "description": "Index of the camera in the PVCAM enumeration (read but not used to address the device — the manager opens the first detected camera).",
      "x-imswitch-kind": "integer",
      "x-imswitch-source": [
        "code:required",
        "docs:integer"
      ]
    },
    "cameraPixelSizeUm": {
      "x-imswitch-nullable": true,
      "x-imswitch-source": [
        "code:optional(get)"
      ]
    }
  },
  "required": [
    "cameraListIndex"
  ],
  "title": "PhotometricsManager managerProperties",
  "type": "object",
  "x-imswitch-classes": [
    "PhotometricsManager",
    "DetectorManager"
  ]
}
